#pragma once
#include <iostream>
#include <regex>
#include <string>
#include <vector>

class fun{
private:
    //void filling(std::string &, std::vector<char *> &);
public:
    fun(std:: string&, std::vector<char *> &);
    //std::vector<char *>p;
};
